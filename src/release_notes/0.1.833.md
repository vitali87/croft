feature: croft theme-import now takes a marketplace reference as well as a file: a publisher.name id, a marketplace URL, or a vscode:extension link downloads the .vsix, lifts out one theme JSON and discards the rest — no extension code is installed or run.
