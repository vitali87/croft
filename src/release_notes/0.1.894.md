fix: a PDF page render through pdftoppm is bounded: the renderer and anything it forked are killed after ten seconds and the open reports the overrun, instead of parking the editor for as long as a hung or slow render takes.
