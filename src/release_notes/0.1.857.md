feature: The terminal now keeps the last minutes of its own output in memory, bounded by bytes rather than by lines, so a future Session: Rewind can replay what a command printed before the screen was cleared — and a runaway flood evicts the oldest output instead of growing without limit.
