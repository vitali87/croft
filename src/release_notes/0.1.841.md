feature: croft can open a workspace on the box a terminal pane is ssh'd into — the palette's "Remote: Open Workspace on This Pane's Host" reads what the pane is actually running and hands the host to the usual remote connect flow.
