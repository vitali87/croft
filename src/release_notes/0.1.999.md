feature: Debugger: hit count breakpoints (pause on the Nth hit), function breakpoints (pause when a named function is entered) and Run to Cursor, from the Command Palette. A debugger that lacks one of these says so.
