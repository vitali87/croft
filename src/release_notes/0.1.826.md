feature: Debug This Test now breaks where the test last failed: croft parses the runner's own failure location (libtest, pytest, jest/vitest) and sets a temporary breakpoint at the assertion, removed when the session ends and never touching a breakpoint you set yourself.
