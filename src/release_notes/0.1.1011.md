feature: croft can read a pull request through `gh`: its files with their changes, its checks and their state (GitHub Actions and commit statuses alike), the logs of failing checks, and which files you marked viewed. This is the groundwork for reviewing pull requests in croft, which arrives in the next release.
