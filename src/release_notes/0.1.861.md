feature: Collapse a terminal pane to a one-column strip and the panes still open take its width, so an idle ssh session stops costing the same room as the build you are watching.
feature: The pane's new chevron button folds it, a click anywhere on the strip brings it back, and Cmd+K ] restores every folded pane to an even split in one gesture.
fix: The minus button still closes the pane it always closed; collapsing got its own control rather than taking that one over.
