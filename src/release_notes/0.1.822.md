feature: Runnable fences in the Markdown preview carry a play glyph: sh, bash, zsh and fish blocks (and python or node when the interpreter is on PATH) run on Cmd+Enter or a click, in a named terminal pane that is reused when it sits idle in the block's own directory, always behind a confirm, with a destructive-looking block flagged in red.
