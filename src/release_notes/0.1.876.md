feature: An open diff view now follows the code: when an agent or a save rewrites the file behind a Source Control, Timeline or two-file diff, the rows update in place with your scroll position kept, and a staged, branch or previous-commit diff re-runs when git status lands (#471).
