fix: a terminal command decoration now carries a stable id, so a caller that remembers one command still names that command after older ones scroll out of the scrollback — a position could silently come to mean a different command.
