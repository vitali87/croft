fix: a terminal command decoration now names the command itself rather than its position, so a caller that remembers one still means that command after older ones scroll out of the scrollback — where a position could silently come to mean a different command.
