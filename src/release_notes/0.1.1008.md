feature: renaming or moving a file in the Explorer now updates the imports and module declarations that point at it, using the edit the language server offers (workspace/willRenameFiles), and tells the server when the move is done. Esc skips the wait, and a file typed into, saved or written by another program while it waits (or just before) keeps its own text.
fix: rename and quick-fix edits now reach a copy of the file open in another editor group when it holds the same text, and a file dropped in from Finder moves through the same server-edit step. Files these edits rewrite on disk are reported to the language servers.
