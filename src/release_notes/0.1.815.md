feature: croft import-vscode brings your colour theme across: when workbench.colorTheme names an installed extension theme, it is converted the way croft theme-import does and croft's theme setting follows it.
