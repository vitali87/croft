feature: the debug picker now follows the order your launch.json asks for. A configuration or compound that sets `presentation` is grouped and ordered the way VS Code would place it, and one that sets `hidden` is left out of the list and its count. A file that says nothing about presentation is listed exactly as written, as before.
