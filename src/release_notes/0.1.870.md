fix: Resizing a terminal pane no longer leaves stale fragments of a full-screen program such as Claude Code on the screen. The grid takes the new size before the child is told about it, so its repaint always lands on a grid of the right shape.
