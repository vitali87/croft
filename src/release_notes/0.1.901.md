feature: the agent review queue now groups each agent's changed files by the workspace root they are in, naming a worktree lane's group by its branch, so changes made in a lane read as their own section.
