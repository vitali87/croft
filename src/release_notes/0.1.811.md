fix: The test suite's load budgets now read a thread count given as `--test-threads`, not only as an environment variable, so a run that spells it with the flag stops timing out waits it had twice the room for.
