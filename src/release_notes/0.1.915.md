feature: `croft view <file>` from any pane opens that file in the croft hosting it, so a shell prompt can ask for the paged PDF, spreadsheet, or SQLite viewer the editor already has.
feature: `croft view -` reads piped bytes and stages them under the right name, so `cat data.csv | croft view -` lands in the sheet grid and an image arrives as an image.
