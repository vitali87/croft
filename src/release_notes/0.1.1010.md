feature: A croft installed with Homebrew is upgraded with Homebrew. The update popup names `brew upgrade croft` instead of offering Update, croft never builds or swaps a binary inside the Cellar, and `croft --build-info` says the install is Homebrew-managed.
