fix: a runnable block's captured output is now tied to its command by a stable id rather than by position in the pane's command list, so a long-running block whose own output scrolls older marks away can no longer show, or write, another command's output.
