feature: The Extensions panel gains a FROM VS CODE group: the workspace's recommended extensions, and (via Extensions: Compare with VS Code) the ones you have installed, each with what croft has built in, can install, or lacks.
