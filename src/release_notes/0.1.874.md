feature: Cmd+P understands a line after the file name: `alpha:236` lands on that line and `alpha:236-239` selects the range, so a reference copied from a review or a stack trace opens where it points.
