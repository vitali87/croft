fix: a `.docx` or `.odt` no longer puts an unbounded zip central-directory parse on the frame loop when its preview is opened; the document route now uses the same pre-parse cap the archive lister has, and a document past it opens in the hex viewer and says why. The cap is a size proxy and errs toward refusing: a large document full of images may now open as hex rather than as a preview.
