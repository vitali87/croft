fix: no user-visible change in this release — it tightens an internal test that had been accepting a malformed markdown code fence alongside the correct one, so a regression in fenced-block output would now be caught rather than passing silently.
