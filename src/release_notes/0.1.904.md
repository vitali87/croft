fix: a zip with an enormous number of members no longer freezes the editor while it is listed; archives past the listing cap open in the hex viewer and say why, so a browser replaced by a hex dump reads as a deliberate limit rather than a bug.
