fix: copying inside Claude Code, tmux or nvim running in a croft terminal now works on a remote box, as it does locally. Those programs copy through OSC 52 when they have no clipboard tool, which is always the case over SSH, and croft's terminal dropped it. The copy now lands where croft's own copy does: your local machine's clipboard in a `croft remote` session.
