feature: csvlens joins the Extensions catalog: add it, and CSV or TSV files get an Open in csvlens entry in the Explorer menu and the Command Palette that opens the file in the terminal spreadsheet viewer in a pane of its own, installing the tool pinned from its own release when it is not on PATH.
