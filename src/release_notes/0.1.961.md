feature: renaming or moving a file in the Explorer now updates the imports and module declarations that point at it, using the edit the language server offers (workspace/willRenameFiles), and tells the server when the move is done. Esc skips the wait.
