fix: a hung network mount (for example a stopped OrbStack machine) no longer prints "nfs server ... not responding" over the UI: the PORTS panel's background socket checks run detached from the terminal and skip the file-system calls that stall on a dead mount.
