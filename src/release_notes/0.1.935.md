feature: a launch.json compound marked `"presentation": { "hidden": true }` is no longer offered in the debug configuration picker, and a compound asking only to be hidden now launches instead of being refused. `group` and `order` still report as unsupported, since the picker has no ordering for them to control.
