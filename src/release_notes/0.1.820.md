feature: a large rendered log opens before its line index is complete: the first 8 MiB are indexed on open and the rest streams in on a background thread, so a multi-gigabyte log paints at once; until the pass finishes the header says how many lines so far, and a find that reaches the moving end reports out of reach rather than no match.
