feature: croft theme-import now takes a marketplace reference as well as a file: a publisher.name id, a marketplace or Open VSX URL, or a vscode:extension link downloads the .vsix, lifts out one theme JSON with any include chain it extends, and discards the rest — no extension code is installed or run.
