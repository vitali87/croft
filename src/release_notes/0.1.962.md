fix: app tests no longer read the user's config.json, through the settings layers or the saved prefs, so a personal preference such as a hidden TIMELINE cannot fail the suite.
fix: turning an MCP extension off and on, or uninstalling and re-adding it, now approves a tool whose definition changed, so the advice in the refusal message works.
