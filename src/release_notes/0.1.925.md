feature: `cargo binstall croft-software` installs a prebuilt binary in seconds instead of compiling for ten minutes, on macOS (arm64, x86_64) and Linux musl (arm64, x86_64).
feature: every tagged release now carries four archives and a `SHA256SUMS` file, so a download can be verified before it is run.
