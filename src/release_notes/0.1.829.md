feature: A runnable fence shows its last run's output underneath it in the Markdown preview, coloured as the pane printed it, with the exit code named when it is not zero. `{persist}` writes that output back into the document as a fence croft replaces on each run, and `{timeout=N}` stops waiting after N seconds without stopping the block.
