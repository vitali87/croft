feature: Cmd+K Shift+L cuts a worktree lane and opens a pane named after it in the new worktree, starting the agent named by `lane_agent` from its agents.json launch line; the lane comes back on relaunch with its agent seated, and closing the lane closes its pane.
