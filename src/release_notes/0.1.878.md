fix: Clicking inside a full-screen app that tracks the mouse now reaches the app: the × on Claude Code's diff panel closes it, and a lazygit panel or a vim split takes the click. Dragging still selects text in an app that only asked for clicks (Claude Code), goes to the app when it asked for motion (vim, lazygit), and Shift+drag always selects with croft (#474).
