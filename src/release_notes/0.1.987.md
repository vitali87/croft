fix: updating a remote croft (fn+F9) no longer leaves a "session host updated" line painted across the editor. The notice that an attached client is older than the updated session now appears in the status bar.
