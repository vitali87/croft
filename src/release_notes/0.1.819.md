feature: CAPTURES: press n on a captured error line (or right-click it) to ask the resident Navigator about it, with the pane rows around it; a path:line in the line opens that file so the answer lands there as a comment box.
