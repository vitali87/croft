feature: Terminal: Fleet Run takes "hosts: command" (or "*: command" for every configured host) and runs it across those ssh hosts at once, reporting to a Fleet output channel which outputs match the fleet's normal, which differ, and which failed.
