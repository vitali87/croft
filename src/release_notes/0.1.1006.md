fix: With two local windows attached to the same `croft remote` session, opening a link, forwarding a port or dropping a file happened once per window (a click opened several browser tabs). Each request is now served by one window.
