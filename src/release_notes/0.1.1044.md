feature: `croft web` serves a workspace's running session over WebSocket on a loopback address. Each connection joins as a normal participant: it is listed in Session: Participants, sees the same output as a terminal client, and leaves when it disconnects. Connections need the token printed at start-up. The browser page itself is still to come.
