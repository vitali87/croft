feature: croft tracks a symbol's byte range as you edit, so a view pinned to one function grows and shrinks with it instead of sliding off, and reports the symbol as gone rather than re-anchoring when an edit removes it.
