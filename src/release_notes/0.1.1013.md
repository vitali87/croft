feature: Session: Detach (Cmd+K Shift+A) leaves a persistent session without closing the window; the session keeps running and read-only participants can use it too.
fix: a window detached or disconnected from a persistent session gets its shell back in a usable state instead of being left in croft's full-screen and mouse modes.
