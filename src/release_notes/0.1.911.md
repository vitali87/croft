fix: opening a PDF no longer risks parking the editor while a page-count probe hangs; the probes are now bounded, and a document whose count cannot be read opens on its first page instead of freezing.
