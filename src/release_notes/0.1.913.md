fix: Ctrl+B now reaches the program running in a focused terminal pane instead of toggling the side bar, so Claude Code's "background this command" works inside croft. The side bar keeps Cmd+B, the command palette and the View menu; everywhere outside the terminal, Ctrl+B is unchanged.
