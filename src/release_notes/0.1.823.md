feature: .http/.rest files: Cmd+Enter sends the request under the caret (### blocks, {{variables}} from a .http.env.json beside the file); the response opens as a tab with status and headers as comments and JSON pretty-printed, images render inline, and history keeps the raw line so secrets stay in the env file.
