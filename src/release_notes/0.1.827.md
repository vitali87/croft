fix: a task is no longer typed into a pane whose shell has walked away from the task's directory, or into one whose directory croft cannot read; the runnable-fence and task paths now share one predicate instead of two copies that drifted apart.
