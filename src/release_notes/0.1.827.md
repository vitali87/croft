fix: a task is no longer typed into a pane whose shell has walked away from the task's directory. A pane whose directory croft cannot read at all (Android, a remote pane's ssh process) is still reused as before — "unknowable" is not "moved away". The runnable-fence and task paths now share one predicate instead of two copies that drifted apart.
