fix: two terminal panes could paint into each other's rows after maximizing, leaving characters from one pane embedded inside words of the other and text over the sidebar; the corruption survived a repaint because nothing tracked those writes. A pane the frame does not draw now drops the rectangle the image overlay anchored to.
