feature: the Explorer has an AGENT LANE section. It lists each coding agent that changed files and, under it, those files, with unreviewed ones first and marked. Clicking a file opens its diff against the version you last reviewed. `Cmd+K V` opens the section, which can also be shown or hidden from the EXPLORER ⋯ menu. It takes no room until an agent has changed something.
