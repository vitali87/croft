feature: Agent: New Worktree Lane creates a git worktree on its own branch beside the repo and adds it as a workspace folder; closing a lane refuses to remove a worktree with uncommitted work and says what is in it.
