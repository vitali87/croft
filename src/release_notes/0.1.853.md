feature: Session: Record Terminal as Asciicast writes the active terminal to a .cast file that asciinema and the agg GIF converter read directly, including a resize event when the pane changes size mid-recording.
