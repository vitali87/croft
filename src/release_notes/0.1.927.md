feature: "Problems: Check Whole Project" runs your TypeScript project's compiler and lists every error it finds in PROBLEMS, including in files you never opened. Running it again replaces the previous results, so a fixed file's row disappears. If the compiler cannot run, croft says why rather than reporting a clean project.
