feature: `croft hook install` registers croft as Claude Code's approval hook for file edits. `--global` installs it for every project, and `--uninstall` removes it, restoring the settings file byte-for-byte when nothing else has changed it. The approval popup that answers the hook comes next. Until then, and whenever croft is closed or silent for 120 s, Claude Code's own prompt decides as before.
