fix: The Cmd+Z directory jump no longer stalls for seconds after Enter: the old workspace's language servers are shut down on a background thread instead of the UI waiting on them, so the tree, terminal and title move the moment you pick a directory.
fix: Typing into the jump popup no longer spawns zoxide twice per keystroke; the frecency list is captured once at open and filtered in-process with zoxide's own keyword rule, so each key costs microseconds and the results are unchanged.
