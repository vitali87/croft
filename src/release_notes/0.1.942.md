feature: a launch.json compound now runs. Selecting one starts every configuration it names, in the order the file lists them, and the Run and Debug view says which sessions are live and which one the call stack belongs to. Stopping stops all of them, and a member that fails to start no longer takes its siblings down with it. A member's own preLaunchTask is skipped for now, with a note saying so.
