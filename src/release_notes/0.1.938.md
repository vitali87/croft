fix: `Cmd+C` in a terminal on a remote box now reaches the clipboard of the machine you are typing at. croft was writing the REMOTE box's clipboard, which an SSH session usually has no tool for at all, and falling back to an OSC 52 sequence the host terminal is free to ignore, while the status line reported success either way. Copied text now travels home over the same drop relay a paste already uses to fetch it.
fix: a copy that finds nothing selected says so instead of doing nothing in silence, and names Shift+drag as the gesture that selects inside a program that tracks the mouse, which is every full-screen TUI. A plain drag there belongs to the program, so croft holds no selection of its own to copy.
