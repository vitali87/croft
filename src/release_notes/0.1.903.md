feature: a Source Control diff can be grouped by seat: who wrote each line the change adds, as a coloured bar in the right gutter and a per-seat tally in the header, so a review reads as the agent's part and your part; lines croft did not watch being written are counted as unrecorded rather than credited to anyone.
